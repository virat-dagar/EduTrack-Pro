# EduTrack Pro Frontend

React + Vite frontend for EduTrack Pro.

## Run

```bash
npm install
npm run dev
```

## Build

```bash
npm run lint
npm run build
```

Set `VITE_API_BASE_URL` when the backend is not running at `http://localhost:8000/api/v1`.
